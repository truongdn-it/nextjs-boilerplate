/* eslint-disable no-unused-vars */
interface DataType {
  id: string;
  label: string;
  priority: string;
  status: string;
  title: string;
}

interface ITask {
  id: string;
  title: string;
  status: string;
  label: string;
  priority: string;
}
